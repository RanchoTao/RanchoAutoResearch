# Table index

- `key_results.tex`: current paper's selected validated result table; generated during manuscript assembly from the frozen numerical ledger.
- Complete row-level result tables are CSV files under `../results/`.
- The canonical manuscript number-to-source mapping is `../paper/current/numerical_provenance.csv`.
