# Analysis plan

1. Verify the ARC-006 integrity lock and copy its outcome-free target manifest.
2. Run identical tokens through intact, block-deleted, and sealed activation-
   noise paths; save per-example compressed arrays and cell summaries.
3. Validate recomputed KL/NLL/`D_S`, key grain, finite values, harness checks,
   and the `delta_margin=delta_b` identity.
4. Before joining outcomes, create and commit the deterministic geometry-match
   manifest using the frozen calipers.
5. Run the ARC-006-compatible composite-damage M0-M4 sequence, separate-KL/NLL
   collinearity sensitivity, run bootstrap, leave-one-run/layer checks, margin
   strata, layer analysis, sign-reversal prediction, and multicollinearity
   audit exactly as preregistered.
6. Generate seven static publication-draft figures from saved processed data,
   inspect every exported image, and preserve the strongest counterevidence.

The paired cell is the analytical grain; the independent training run is the
inferential grain. No token-level p-values are used.
