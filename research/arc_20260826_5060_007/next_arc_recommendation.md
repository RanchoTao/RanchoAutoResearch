# Next ARC recommendation

Recommend exactly one next ARC:

## ARC-006R — deterministic top-1 assay repair and reseal

Before any geometry work, pre-register a canonical deterministic top-1 rule
and apply it to both block and noise families on the same cached logits/tokens.
Recompute all 150 outcomes, preserve the existing inverse-targets and Class A
matching contract where logically possible, and repeat the frozen run-level
residual, equivalence, leave-one-run-out, layer, and sign-reversal analyses.
Use the alternative tie rule only as a named sensitivity analysis.

GO back to ARC-007 only if the corrected residual remains outside equivalence,
has the same direction in at least 4/5 runs, and its main layer/sign structure
is not dominated by tie-rule-sensitive cells. Otherwise narrow or stop
Candidate A rather than adding another geometry metric.
