# Multicollinearity audit — not run

The preregistration documented `corr(KL,NLL)=0.997675` and froze an equal-weight
standardized damage composite before extraction. No geometry model, VIF,
condition-number verdict, or coefficient-stability analysis was run because
the prior-assay outcome failed the required reproduction gate first.

Running these diagnostics against invalid cell residuals would not answer the
ARC-007 question.
