# META-ARC-05 — Low-Compute Replication and Boundary Explorer

This directory is isolated from all earlier ARC runs. The workflow is fixed as:

`published anchor → competence → replication → boundary map → held-out test`.

ARC folders use machine/date-scoped names beginning with
`ARC-20260825-5060-001`.

